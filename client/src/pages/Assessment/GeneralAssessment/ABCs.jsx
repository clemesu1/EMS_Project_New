import React from 'react';
import { Grid } from '@mui/material';

const ABCs = () => {
  return (
    <Grid container spacing={3}>
      <Grid item xs={12}>
        ABC's
      </Grid>
    </Grid>
  );
};

export default ABCs;
